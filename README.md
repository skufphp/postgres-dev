# PostgreSQL Dev Environment

Локальное окружение для работы с PostgreSQL и pgAdmin в Docker. Проект предназначен для централизованного управления базами данных для нескольких локальных проектов.

## 🚀 Быстрый старт

1. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env` при необходимости (пароли, порты).

2. **Запустите контейнеры:**
   ```bash
   make up
   ```
   Или через Docker Compose:
   ```bash
   docker compose up -d
   ```

3. **Проверьте статус:**
   ```bash
   make status
   ```

## 🔌 Подключение к базам

### Из IDE (PhpStorm / GoLand / DataGrip)
Для подключения к основной БД, созданной при старте:

* **Host:** `localhost`
* **Port:** `7031` (или ваш `DB_FORWARD_PORT` из `.env`)
* **Database:** `dev_db` (или ваш `POSTGRES_DB`)
* **User:** `dev_user` (или ваш `POSTGRES_USER`)
* **Password:** `3179019` (или ваш `POSTGRES_PASSWORD`)

### Через pgAdmin 4
Веб-интерфейс доступен по адресу: [http://localhost:7032](http://localhost:7032) (порт `PGADMIN_PORT` в `.env`).

**Данные для входа в pgAdmin:**
* **Email:** `admin@example.com`
* **Password:** `admin`

**Добавление сервера в pgAdmin:**
1. Нажмите "Add New Server".
2. Вкладка **General**: Name — `Postgres Dev`.
3. Вкладка **Connection**:
   * **Host name/address:** `postgres-dev` (если используете Docker Compose сети) или `localhost` (если pgAdmin запущен отдельно).
   * **Port:** `5432` (внутри Docker-сети) или `7031` (с хоста).
   * **Maintenance database:** `postgres` (или ваша стартовая БД).
   * **Username:** `postgres` (или ваш `POSTGRES_USER`).
   * **Password:** Ваш `POSTGRES_PASSWORD`.

## 📂 Создание баз “под проекты” (1 PostgreSQL на VPS/сервер, но отдельная БД на каждый проект)

Идея простая и надёжная:

- **PostgreSQL-сервер один** (не плодим контейнеры/сервисы).
- **Каждый проект живёт в своей отдельной базе**.
- **У каждого проекта свой пользователь** (логин/пароль).
- Laravel миграции создают таблицы **в `public` своей базы** — это дефолт и так задумано в Postgres/Laravel.
- Проекты изолированы: миграции/ошибки/удаления не задевают соседей.

---

## ✅ Что важно понимать заранее

### 1) Laravel **не создаёт базы данных**
Команда `php artisan migrate` создаёт **таблицы** (например, `migrations`, `users`, `jobs`), но **не** делает `CREATE DATABASE`.

Поэтому базу для проекта нужно создать заранее (вручную SQL или через админку).

### 2) У каждой базы **свой `public`**
В PostgreSQL схема `public` существует **внутри каждой базы отдельно**.

То есть:
- `project1_db.public.users` и
- `project2_db.public.users`

— это разные таблицы в разных базах, они не конфликтуют.

---

### 🧱 Шаблон: “1 проект = 1 база + 1 пользователь”

Пример для проекта `app1`:

- база: `app1_db`
- пользователь: `app1_user`
- пароль: `<PASSWORD_PLACEHOLDER>`

#### Вариант A — через SQL (рекомендуется)

#### 0) Подключитесь суперпользователем
Подключитесь к PostgreSQL под админом (обычно `postgres`) через psql/pgAdmin/DataGrip.

#### 1) Создайте пользователя проекта
```sql
CREATE ROLE app1_user WITH LOGIN PASSWORD '<PASSWORD_PLACEHOLDER>';
```

#### 2) Создайте базу проекта и назначьте владельца
```sql
CREATE DATABASE app1_db OWNER app1_user;
```

#### 3) (Рекомендуется) Закройте доступ “всем” и оставьте только владельцу
Это снижает риск, что кто-то случайно подключится к чужой базе.

```sql
REVOKE ALL ON DATABASE app1_db FROM PUBLIC;
GRANT CONNECT ON DATABASE app1_db TO app1_user;
```

Готово: у проекта есть своя база и пользователь, который может в ней работать.

---

### 🔌 Подключение Laravel-проекта к новой базе

В `.env` проекта выставьте:

```dotenv
DB_CONNECTION=pgsql
DB_HOST=localhost
DB_PORT=7031
DB_DATABASE=app1_db
DB_USERNAME=app1_user
DB_PASSWORD=<PASSWORD_PLACEHOLDER>
```

После изменения `.env` обязательно сбросьте кэш конфигурации (Laravel это любит):

```bash
php artisan config:clear
php artisan cache:clear
```

Дальше запускайте миграции:

```bash
php artisan migrate
```


Ожидаемый результат:
- Laravel создаст таблицу `migrations`
- затем создаст остальные таблицы из миграций
- при повторном запуске будет: `Nothing to migrate.`

---

### 🔎 Проверка, что таблицы реально создались (SQL)
Подключитесь к **`app1_db`** и выполните:

```sql
SELECT tablename
FROM pg_catalog.pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;
```

Вы должны увидеть как минимум: `migrations`, `users`, `jobs` и т.п. (зависит от набора миграций).

---

### 🧰 Если в PhpStorm/DataGrip “миграции прошли, а таблиц не видно”
Это часто не баг, а настройка отображения.

**Симптом:** в дереве базы написано что-то вроде “No schemas selected”.

**Решение:**
1) Database tool window → ваш datasource → база `app1_db`
2) Откройте **Schemas…** (или Properties → Schemas)
3) Отметьте схему **`public`**
4) Нажмите **Apply / OK**
5) Сделайте **Synchronize / Refresh**

После этого таблицы появятся в дереве.

---

### 🧹 Удаление учебного проекта (когда больше не нужен)

> Внимание: удаление необратимо.

```sql
DROP DATABASE app1_db;
DROP ROLE app1_user;
```


---

### Частые ошибки и их причины

#### Ошибка: `permission denied for schema public`
Почти всегда означает одно из двух:
- проект подключился **не тем пользователем** (у которого нет прав),
- или вы вручную отозвали права на `public` в этой базе.

**Как избежать:** делайте пользователя владельцем базы (`OWNER`) и не трогайте `search_path`/schema-настройки, если используете модель “1 база на проект”.

---

### 📌 Итог
Для учебных проектов самый простой и безопасный подход:

- один PostgreSQL-сервер,
- отдельная база на проект,
- отдельный пользователь на проект,
- миграции работают в `public` внутри своей базы.

## ⚠️ Важно: Первый старт и Volume

Переменные `POSTGRES_PASSWORD`, `POSTGRES_USER` и `POSTGRES_DB` используются **только при первой инициализации** пустого хранилища (volume). 

* Если вы уже запускали контейнер, а затем поменяли пароль или пользователя в `.env`, в базе они **не изменятся**.
* Чтобы начать с "чистого листа" и применить новые настройки из `.env`, нужно удалить volume:
  ```bash
  make clean
  ```
  **Внимание:** Это действие удалит все созданные вами базы данных и данные в них!

## 🛠 Команды Makefile

* `make up` — запустить контейнеры.
* `make down` — остановить контейнеры.
* `make status` — проверить состояние контейнеров.
* `make logs` — просмотр логов.
* `make shell` — зайти в psql внутри контейнера.
* `make clean` — полная очистка (удаление контейнеров и данных!).
